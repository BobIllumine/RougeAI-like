using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Status {
    OK,
    STUNNED,
    ENSNARED,
    MUTE,
    DISARMED
}

public enum ActionStatus {
    IDLE,
    ATTACK,
    RUN,
    JUMP,
    HURT,
    FALL
}
public enum ErrCode
{
    INVALID_CAST,
    OK
}
